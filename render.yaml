services:
  - type: web
    name: metabase-deploy
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: MB_JETTY_PORT
        value: ${PORT}
      - key: JAVA_TOOL_OPTIONS
        value: "-Xmx350m"
